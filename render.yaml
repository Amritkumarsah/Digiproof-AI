services:
  - type: web
    name: digiproof-backend
    env: docker
    region: ohio
    plan: free
    rootDir: backend
    envVars:
      - key: GEMINI_API_KEY
        sync: false # Set this securely in the Render Dashboard
      - key: PORT
        value: 8000
